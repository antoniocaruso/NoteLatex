\begin{table}\sffamily\centering
\renewcommand{\arraystretch}{1.2}
\begin{pycode}
print(r"\begin{tabular}{l|rccc}")
print(r"Binario & Posizionale & Modulo e Segno & Scostamento (a $7$) & Complemento a $2$ \\ ")
for i in range(16):
        print(f"{i:04b} & {i:2d} & {'+' if i<8 else '-'}{i if i<8 else i-8}  & {i-7:+2d} & {i if i<=2**3-1 else -2**4+i:+d} \\\\")
print(r" \hline\noalign{\smallskip}")
print(r"\end{tabular}")
\end{pycode}
\caption{Numeri binari di $4$ bit: valore rispetto alla notazione posizionale (naturali), e confronto dei valori nei vari sistemi per numeri negativi.}
\label{tab:zeta}
\end{table}
